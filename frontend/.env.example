# Base URL of the backend API. In development, Vite proxies /api to
# http://localhost:5000 automatically (see vite.config.js), so this
# usually doesn't need to be set locally. Set it for production builds.
VITE_API_URL=/api
